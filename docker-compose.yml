services:
  db:
    image: timescale/timescaledb:latest-pg16
    environment:
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - PGDATA=/var/lib/postgresql/data/pgdata
    volumes:
      - ./core-schema.sql:/docker-entrypoint-initdb.d/core-schema.sql
      - ./metrics-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    networks:
      - kanshi-network

  core:
    image: johnyoat/kanshi-core:latest
    env_file:
      - .env
    ports:
      - "8080:8080"
      - "50051:50051"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - kanshi-network
    restart: always

  dashboard:
    image: johnyoat/kanshi-ui:latest
    ports:
      - "80:80"
    depends_on:
      core:
        condition: service_started
    networks:
      - kanshi-network
    restart: always

networks:
  kanshi-network:
    driver: bridge